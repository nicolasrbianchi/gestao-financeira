import React from 'react';import { BarChart,Bar,XAxis,YAxis,ResponsiveContainer,Tooltip } from 'recharts';import { money } from '../utils/format';
export default function Categories({data,loading}){ if(loading||!data) return <p className='loading-state'>Carregando...</p>; if(!data.byCategory?.length) return <div className='card'>Sem dados de categorias no período.</div>; const total=data.byCategory.reduce((a,c)=>a+c.value,0); return <div><div className='card h'><h3>Despesas por categoria</h3><ResponsiveContainer width='100%' height={220}><BarChart data={data.expensesByCategory}><XAxis dataKey='name'/><YAxis/><Tooltip formatter={(v)=>money(v)}/><Bar dataKey='value' fill='#f43f5e'/></BarChart></ResponsiveContainer></div>{data.byCategory.map((c)=><div className='card' key={c.name}><p>{c.name} <b>{((c.value/total)*100).toFixed(1)}%</b></p><div className='progress'><span style={{width:`${(c.value/total)*100}%`}}/></div><p>{money(c.value)}</p></div>)}</div>}
