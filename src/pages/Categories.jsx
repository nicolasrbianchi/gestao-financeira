import React from 'react';import { BarChart,Bar,XAxis,YAxis,ResponsiveContainer,Tooltip } from 'recharts';import { money } from '../utils/format';
export default function Categories({data,loading}){ if(loading||!data) return <p>Carregando...</p>; return <div><div className='card h'><h3>Despesas por categoria</h3><ResponsiveContainer width='100%' height={220}><BarChart data={data.expensesByCategory}><XAxis dataKey='name'/><YAxis/><Tooltip/><Bar dataKey='value' fill='#f43f5e'/></BarChart></ResponsiveContainer></div>{data.byCategory.map((c)=><div className='card' key={c.name}>{c.name}: {money(c.value)}</div>)}</div>}
