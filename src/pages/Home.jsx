import React from 'react';import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, AreaChart, Area } from 'recharts';import { money } from '../utils/format';
const tt=(v)=>money(v);
export default function Home({data,loading}){ if(loading||!data) return <p className='loading-state'>Carregando...</p>; return <div><div className='grid'>{data.summaryCards.map((c)=><div className='card' key={c.key}><small>{c.title}</small><strong>{money(c.value)}</strong><p className='muted'>{c.helper}</p></div>)}</div><div className='card h'><h3>Receita vs Despesa</h3><ResponsiveContainer width='100%' height={220}><BarChart data={data.charts.incomeExpense}><XAxis dataKey='name'/><YAxis tickFormatter={tt}/><Tooltip formatter={tt}/><Bar dataKey='value' fill='#5b8cff'/></BarChart></ResponsiveContainer></div><div className='card h'><h3>Evolução diária</h3><ResponsiveContainer width='100%' height={220}><AreaChart data={data.charts.dailySeries}><XAxis dataKey='date'/><YAxis tickFormatter={tt}/><Tooltip formatter={tt}/><Area dataKey='receitas' stroke='#10b981' fill='#10b98122'/><Area dataKey='despesas' stroke='#f43f5e' fill='#f43f5e22'/></AreaChart></ResponsiveContainer></div></div>}
