import React, { useState } from 'react';
export default function More({api,onLogout}){const [h,setH]=useState(''); const [meta,setMeta]=useState(''); return <div><div className='card'><p>Status</p><button onClick={async()=>{const r=await api('/health');setH(r.appsScript?.ok?'API e Apps Script conectados':'API conectada com falha no Apps Script');setMeta(`v${r.app?.version} · ${r.nodeEnv}`)}}>Testar conexão</button><p>{h}</p><p>{meta}</p><button onClick={onLogout}>Sair da sessão</button></div></div>}
