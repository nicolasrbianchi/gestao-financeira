import React from 'react';import { money } from '../utils/format';
export default function Transactions({data,loading,filters,setFilters}){ if(loading||!data) return <p>Carregando...</p>; return <div><input placeholder='Buscar nome' value={filters.search||''} onChange={(e)=>setFilters({...filters,search:e.target.value})}/><p>Total: {money(data.summary.totalAmount)} · {data.summary.count} itens</p>{data.transactions.map((t)=><div key={`${t.sheetRowNumber}-${t.name}`} className='card'><strong>{t.name}</strong><p>{money(t.amount)} · {t.type} · {t.displayDate}</p><p>{t.account} · {t.category} / {t.subcategory}</p><p>{t.paymentMethod} · {t.status}</p>{t.installment&&<p>Parcela: {t.installment}</p>}{t.notes&&<p>Obs: {t.notes}</p>}</div>)}</div>}
